import React from "react";
import styled from "styled-components";

const Footer = styled.div`
  width: 100vw;
  height: 400px;
  margin: 0 auto;
  background-color: rgba(10, 80, 90, 0.8);
`;

export default () => <Footer>This is footer</Footer>;
